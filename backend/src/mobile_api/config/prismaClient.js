// Prisma Client - Not used in this project
// This file is kept for compatibility but Prisma is not installed
// The project uses PostgreSQL client (pg) directly

const prisma = {
  $disconnect: async () => {},
  // Prisma is not used - all database operations use pg client
};

module.exports = prisma;

